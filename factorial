
#include <stdio.h>
int main() { 
    int n, f=1;
    
    printf ("Whose factorial You want to know: ");
    scanf ("%d", &n);
    
    for (int i=1; i<=n; i++) {
      f*=i;
    }
    
    printf ("%d! = %d", n, f);
}
